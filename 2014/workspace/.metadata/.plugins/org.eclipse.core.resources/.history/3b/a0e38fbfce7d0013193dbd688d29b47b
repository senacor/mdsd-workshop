/*
 * generated by Xtext
 */
package com.senacor.mdsd.hh.greeter.generator

import org.eclipse.emf.ecore.resource.Resource
import org.eclipse.xtext.generator.IGenerator
import org.eclipse.xtext.generator.IFileSystemAccess
import com.senacor.mdsd.hh.greeter.greeter.Greeting

/**
 * Generates code from your model files on save.
 * 
 * see http://www.eclipse.org/Xtext/documentation.html#TutorialCodeGeneration
 */
class GreeterGenerator implements IGenerator {
	
	override void doGenerate(Resource resource, IFileSystemAccess fsa) {
		for (greeting: resource.allContents.toIterable.filter(Greeting)) {
			fsa.generateFile("/my/generation"+greeting.name+"Greeter.java", greeterClass(greeting))
		}
//		fsa.generateFile('greetings.txt', 'People to greet: ' + 
//			resource.allContents
//				.filter(typeof(Greeting))
//				.map[name]
//				.join(', '))
	}
	
	def greeterClass(Greeting greeting) {
		'''
		package my.generation;
		
		public class «greeting.name»Greeter {
		  public static void doGreeting() {
		  	System.out.println("Hallo, «greeting.name»!");
		  }
		}
		'''
	} 
}
